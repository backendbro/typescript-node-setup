{
  "name": "typescript-setup",
  "version": "1.0.0",
  "description": "A typescript-node set-up",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"node dist/server.js",
    "dev":"nodemon src/server.ts",
    "build":"tsc -p ."  
  },
  "author": "backendbro",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
